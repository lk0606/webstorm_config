import * as React from 'react';

class ${NAME} extends React.Component {
constructor(props:any) {
        super(props)
        // 数据
        this.state = {
        }
    }
  public render() {
    return (
        <div>
        
        </div>
    )
  }
}

export default ${NAME}